meta {
  name: 00 Readyz
  type: http
  seq: 2
}

get {
  url: http://localhost:4000/api/readyz
  body: none
  auth: none
}

tests {
  test("returns 200 when dependencies are ready", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("dependencies are ready", function() {
    const body = res.getBody();
    expect(body.status).to.equal("ok");
  });
}
