meta {
  name: 05 Delete Todo
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/todos/{{todoId}}
  body: none
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("returns deleted todo", function() {
    const body = res.getBody();
    expect(body.id).to.equal(bru.getEnvVar("todoId"));
  });
}
