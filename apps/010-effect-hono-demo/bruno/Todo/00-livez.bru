meta {
  name: 00 Livez
  type: http
  seq: 1
}

get {
  url: http://localhost:4000/api/livez
  body: none
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("process is live", function() {
    const body = res.getBody();
    expect(body.status).to.equal("ok");
  });
}
